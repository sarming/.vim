git clone git@github.com:sarming/.vim.git

ln -s .vim/vimrc .vimrc
ln -s .vim/gvimrc .gvimrc

cd .vim
git submodule update --init

git submodule add <url> pack/ian/start/<dir>

git submodule foreach git pull
